apiVersion: kafka.nais.io/v1
kind: Topic
metadata:
  name: jfr-generell-q2-generelloperations-changelog
  namespace: isa
  labels:
    team: isa
  annotations:
    dcat.data.nav.no/title: "isa.jfr-generell-q2-infotrygdoperations-changelog"
    dcat.data.nav.no/description: "Topic for bruk til KeyValueStore i jfr-generell for generellOperation"
    kafka.nais.io/removeDataWhenResourceIsDeleted: "true"
spec:
  pool: nav-dev
  config:
    cleanupPolicy: delete
    minimumInSyncReplicas: 1
    partitions: 3
    replication: 2
    retentionBytes: -1    # Messages will never get deleted because of disk space
    retentionHours: 2192  # Messages are stored for 3 months
  acl:
    - team: isa
      application: jfr-generell-q2
      access: readwrite